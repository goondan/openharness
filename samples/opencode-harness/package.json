{
  "name": "opencode-harness-sample",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node -e \"process.exit(0)\"",
    "clean": "rm -rf dist",
    "start": "node ../../packages/cli/dist/bin.js --workdir ."
  },
  "dependencies": {
    "@ai-sdk/anthropic": "^3.0.44",
    "@ai-sdk/google": "^3.0.29",
    "@ai-sdk/openai": "^3.0.29",
    "@goondan/openharness": "workspace:*",
    "@goondan/openharness-base": "workspace:*",
    "ai": "^6.0.86",
    "turndown": "^7.2.1",
    "yaml": "^2.8.1"
  },
  "devDependencies": {
    "@types/turndown": "^5.0.5"
  }
}
